function trainedNetwork = HNNProto(m, n)
%This function does something

%Create a random set of complex inputs for neural network training, based
%on the size of the input vector sizes (m,n)
r = exp(1*i*pi*rand(m,n));

%train the network with the new input
r =
